import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import BlockBlasterSolver from './BlockBlasterSolver';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <BlockBlasterSolver />
  </React.StrictMode>
);
